package com.yolo.fun_habit_journal.framework.datasource.preferences

class PreferenceKeys {

    companion object {
        // Shared Preference Files:
        const val HABIT_PREFERENCES: String = "com.yolo.fun_habit_journal"

        // Shared Preference Keys
        val HABIT_FILTER: String = "${HABIT_PREFERENCES}.HABIT_FILTER"
        val HABIT_ORDER: String = "${HABIT_PREFERENCES}.HABIT_ORDER"
    }
}
